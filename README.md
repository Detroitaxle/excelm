# Excelm – TSV Returns Tracker

Upload TSV, check FedEx, manage in Excel-like sheet.

## Setup
1. Add env vars in Vercel.
2. Run migration: supabase db push.
3. Deployed at [your Vercel URL].

## Features
- TSV upload (skips blank tracking).
- FedEx check (auto-deletes non-delivered).
- Grid with filters, colors, export.
- Dark/light theme.
